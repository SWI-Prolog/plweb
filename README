---+ The new SWI-Prolog web-site

Finally, SWI-Prolog hosts itself on the web. The official address is
(for now) http://prolog.cs.vu.nl


---++ TODO

    * Provide last-modified support for wiki pages
    * Support bug-reporting
    * Downloads
	* Use thread-pool for scalability
	* Support download statistics
	    * By version, platform/src
	    * By time period
    * Allow Wiki editing?
	* Local-host only (like PlDoc)?
	* Wiki-style
    * Process .txt links in @see (See PceEmacs in FAQ)

Update Apache:

    * /usr/local/apache2/bin/apachectl -k graceful
